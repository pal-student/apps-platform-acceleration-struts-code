spring:
  jpa:
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

  datasource:
    url: jdbc:mysql://localhost:3306/struts?useSSL=false
    username: pal
    password: pal

  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp

logging.level:
  root: ERROR
  org.superbiz.struts: DEBUG
  org.springframework.web: DEBUG
